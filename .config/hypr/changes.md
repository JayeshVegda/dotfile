Fix duplicate variables

Add window resizing (Super + R)
Add toggle floating (Super + Shift + F)
Add center floating window

Assign apps to specific workspaces (browser to workspace 2, terminal to workspace 1, etc.)
spotify 4
gurk 2
cursor 3



Float small utility windows (pavucontrol, bluetuith
Add size/position rules for floating windows

Consider disable_hyprland_logo = true if you don't want the default wallpaper


Add focus animation improvements
Add rules for XWayland apps
Better handling of fullscreen apps

